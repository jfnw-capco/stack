{
  "variables": {
    "do_api_token": "{{env `DIGITALOCEAN_ACCESS_TOKEN`}}"
  },
  "builders": [{
    "type": "digitalocean",
    "api_token": "{{user `do_api_token`}}",
    "droplet_name" "test_node",
    "image": "ubuntu-16-04-x64",
    "region": "lon1",
    "size": "512mb",
    "ssh_username": "root"
  }],
  "post-processors": [{
    "type": "manifest",
    "output": "node_manifest.json",
    "strip_path": true
  }]
}
