{
  "variables": {
    "do_token": "",
    "branch": "",
    "region": "",
    "namepace": "",
    "app": "",
    "image": ""
  },
  "builders": [{
    "type": "digitalocean",
    "api_token": "{{user `do_token`}}",
    "image": "docker-16-04",
    "region": "{{user `region`}}",
    "size": "1gb",
    "droplet_name": "{{user `namespace`}}-{{user `app`}}-{{user `image`}}-{{user `branch`}}",
    "snapshot_name": "{{user `namespace`}}-{{user `app`}}-{{user `image`}}-{{user `branch`}}",
    "ssh_username": "root"
  }],
  "provisioners":  [
    {
      "type": "shell",
      "inline": [
        "sudo mkdir /app"
      ]},
    {
      "type": "file",
      "source": "../../dev/",
      "destination": "/app"
    }
  ],
  "post-processors": [{
    "type": "manifest",
    "output": "{{user `image`}}_manifest.json",
    "strip_path": true
  }]
}